{
  "name": "streamflix-web",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name=streamflix-web"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
